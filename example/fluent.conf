<source>
  @type forward
  @id forward_input
</source>

<match example>
  @type google_sheets

  credential_file_path service-account-file.json
  spreadsheet_id "#{ENV['SPREADSHEET_ID']}"
  keys foo
</match>
